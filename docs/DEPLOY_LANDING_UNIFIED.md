# Landing unificata (/it = Next.js)

- **vercel.json**: i rewrites da `/it` e `/it/` verso `landing.html` sono stati rimossi. `/it` è ora gestita da Next.js come le altre lingue (`/en`, `/de`, `/es`, `/fr`).
- **public/landing.html**: può essere rimosso dopo il deploy (opzionale); non è più servito.
- **Test**: dopo il deploy, verificare `/it` e `/en`: stessi layout luxury (indigo/rose gold, Playfair/Inter, sezioni Hero → Target → Benefits → Setup → Pricing → Why Us → CTA).
